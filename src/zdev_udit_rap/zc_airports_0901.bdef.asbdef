projection implementation in class ZBP_C_AIRPORTS_0901 unique;
strict ( 2 );
use draft;
define behavior for ZC_AIRPORTS_0901 alias Airport
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}