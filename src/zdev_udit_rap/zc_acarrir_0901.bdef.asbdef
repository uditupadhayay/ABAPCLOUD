projection implementation in class ZBP_C_ACARRIR_0901 unique;
strict ( 2 );
use draft;
define behavior for ZC_ACARRIR_0901 alias Carrier
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}